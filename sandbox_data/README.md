# Sandbox Data

This directory is the default portable asset root for the offline Hyper-V sandbox.

Runtime assets should stay here instead of arbitrary host locations.

Planned subdirectories:

1. `base_images/`
2. `task_media/`
3. `hyperv/`
4. `exports/`
5. `iso/`
